PORT=3000
DB_FILE=./data.db
JWT_SECRET=replace_with_strong_random_value
ADMIN_KEY=replace_with_strong_admin_key
FRONTEND_URL=http://localhost:3000

# Optional SMTP (recommended for production)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password-or-app-password
# SMTP_FROM="Reels Auto-Poster <no-reply@yourdomain.com>"

# Optional test password (used by send-email convenience)
# TEST_PASSWORD=TestAccess123
